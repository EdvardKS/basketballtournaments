// Placeholder — implemented in Sprint 4.
import { Router } from "express";
export const playersRouter = Router();
playersRouter.get("/", (_req, res) => res.status(501).json({ error: "NOT_IMPLEMENTED" }));
