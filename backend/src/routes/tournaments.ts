// Placeholder — implemented in Sprint 5.
import { Router } from "express";
export const tournamentsRouter = Router();
tournamentsRouter.get("/", (_req, res) => res.status(501).json({ error: "NOT_IMPLEMENTED" }));
