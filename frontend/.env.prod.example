# Frontend production env. PUBLIC_API_BASE is used server-side
# (Astro SSR + middleware proxy) to reach the backend on the Docker network.
PORT=4321
PUBLIC_API_BASE=http://backend:4000
NODE_ENV=production
